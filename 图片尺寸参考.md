# 各栏目图片来源与尺寸参考表

> 用途：后续制作/替换图片时按此标注像素尺寸。所有数值同时给出「CSS 显示比例」和「推荐制作像素」。
> 容器基准：`--max-width: 1680px`，左右内边距 `24px`，即内容可用最大宽 **1632px**（URL 拼：1680 − 24×2）。
> `1rem = 10px`。

---

## 一、顶部 Banner（所有栏目共用，2026-09-07 已加高）

- 样式：`.page-banner`，背景图 `background-size: cover` + 半透明罩层，**无固定宽高，靠内边距撑起**。
- 改动后内边距 `13rem top / 12rem bottom` → 桌面高度约 **250px+**；移动端 `9rem/8rem`。
- 因为 `cover` 铺满，**图片比例不重要但我方强烈建议横幅比例**，否则裁切严重。

| 栏目 | 当前用的图片文件 | 实际像素 | 建议制作尺寸（横幅） |
|------|----------------|----------|----------------------|
| 首页顶部（Hero 大图） | hero-1.jpg / hero-2.webp / hero-3.jpg | 2000×1125 / — / 2000×1333 | **1920×1080（16:9）** |
| 产品列表页 / 产品分类页 / 产品详情页 | banner-products.png | 884×585 | **1632×460 或 1600×500** |
| 新闻/FAQ/Project 列表页 | banner-news.jpg | 1414×885 | **1632×460 或 1600×500** |
| 关于我们页 | about-bg.jpg | 1908×824 | **1632×460 或 1600×500** |
| 联系我们页 | contact-banner.jpg | 1920×670 | **1632×460 或 1600×500** |
| 隐私政策页 | （用默认 banner-news 同级） | — | **1632×460 或 1600×500** |

> Banner 方向建议统一：横版 **16:3.5 ~ 16:4**（约 1600×460），`cover` 裁切后用中上部最安全。

---

## 二、首页各区块

### 1. Hero 轮播大图
- 显示区域：`height: 100vh`（最小 640px），`background-size: cover` + 0.7 暗化。
- 图片文件：hero-1.jpg(2000×1125)、hero-2.webp、hero-3.jpg(2000×1333)。
- 推荐制作：**1920×1080（16:9）**，主体内容居中，避免文字/主体被裁。

### 2. 热销产品卡片（首页 + 产品列表共用）
- 图片容器：`.imgHoverAn img` → `aspect-ratio: 1/1`（正方形）+ `object-fit: cover`。
- 首页每行 4 个、产品列表每行 3 个。
- 推荐制作：**正方形，建议 800×800、更大 1200×1200**（列表卡每张约占总宽 1/3，正方形裁切无剪边）。

### 3. 证书轮播（首页 + 关于页 + 产品详情共用）
- 图片容器：`.cert-item img` → `aspect-ratio: 1/1.24`（竖版）+ `object-fit: contain`（**不裁切，完整显示**）。
- 4 列一页 → 每张约占总宽 1/4 减 1.5rem。
- 推荐制作：**竖版 3:3.7 左右，如 900×1116 或 800×992**；因用 `contain`，比例变形会被"留白"处理，务必按 1:1.24 竖版做。

### 4. Application 行业应用大图
- 图片容器：`.app-bg`（绝对铺满）＋ `.app-track height: 780px`（移动 480px），`background cover`。
- 图片文件：apps/automotive(1920×1084)、consumer-electronics(1109×589)、industrial(1047×696)、telecom(1046×585)、medical(1536×1024)。
- 推荐制作：**1920×780（约 2.46:1 宽扁横幅）**，主体偏右/中（左 22% 有标题文字暗化区）。

### 5. 行业图标（Application 标签）
- 图标容器：`.industry-icon img` → **44×44px**，`object-fit: contain`。
- 必须同时准备两版：**彩色(icon-normal) + 白色(icon-white)**（hover/激活时切换），两张都想换色时用 SVG 最好。
- 推荐制作：**44×44（1:1）；用 SVG 无损 32×32 即可**。现有文件仅 34~44px 小图标，放大会糊，重新做建议用 SVG。

### 6. 新闻卡片（首页 3 列 + 新闻列表共用）
- 图片容器：`.newsImg img` / `.news-card-thumb img` → `aspect-ratio: 16/9` + `object-fit: cover`。
- 推荐制作：**横版 16:9，如 1200×675 或 800×450**（每卡约占总宽 1/3）。

### 7. Our Service（CTA 背景）
- 背景图 `service-bg.jpg` 用于 `.service-cta-bg`，`cover` 铺满。
- 实际像素：1920×960（2:1）。
- 推荐制作：**1920×960 或更宽的 2:1 横幅**。

---

## 三、关于我们页（page-about_us）

| 区块 | 容器显示比例 | 当前文件 | 推荐制作尺寸 |
|------|------------|---------|-------------|
| 公司介绍主图(video poster/图片) | `aspect-ratio 16/9` + cover | about-intro.png(891×588) | **1600×900（16:9）** |
| CNC 左列轮播图 | `aspect-ratio 360/403`（竖版 0.89）desktop 拆列、移动自适应 | workshops/cnc-1~5.png、forging-1.png | **竖版 0.9 左右，如 900×1008** |
| 测试设备大图(featured) | `aspect-ratio 16/9` cover | workshops/testing-*.jpg | **1600×900** |
| 测试设备缩略图 | `aspect-ratio 4/3` cover | 同上 testing-*.jpg(500×417) | **800×600（4:3）** |

---

## 四、产品详情页（single-product）

| 位置 | 显示比例 | 当前文件 | 推荐制作 |
|------|---------|---------|---------|
| 主图(gallery 40% 宽) | `aspect-ratio 3/2` + cover | products/main-1.jpg(790×589) | **1200×800（3:2）** |
| 缩略图列表 | `aspect-ratio 1/1` cover | — | **300×300** |
| 车间图组(workshop-grid) | `aspect-ratio 3/2` cover | workshops/forging-*.png、cnc-*.png、testing-*.jpg | **1200×800（3:2）** |
| 证书轮播 | `aspect-ratio 1/1.24` + contain | certs/*.jpg(竖版 1280×1756~2448×3264) | **900×1116（3:3.7 竖版）** |
| 相关产品(related) | `aspect-ratio 3/2` cover | products/related-*.jpg | **1200×800（3:2）** |

---

## 五、新闻详情 / FAQ / Project 详情侧栏图
- 侧栏推荐位图、小图标：`object-fit` 指定，多沿用 4.2rem~4rem 图标。
- 推荐制作：**侧栏小图 120×80 或正方形 300×300**。

---

## 六、项目列表页（archive-project）
- 缩略图：`.project-card-thumb` → `aspect-ratio 16/10` + cover。
- 推荐制作：**横版 16:10，如 1280×800 或 960×600**。

---

## 七、Logo / 语言标志 / 辅助图标

| 项 | 显示尺寸 | 当前文件 | 备注 |
|----|---------|---------|------|
| 网站 Logo | `max-height 72px`, 宽自适应 | logo.png(469×197)、logo-1.png | 制作建议 **高度 100~150px 的透明 PNG/SVG**，白底 logo 可换 |
| 语言国旗 | 主按钮 28×19px、下拉 24×16px | flags/*.png(128×128) | `object-fit cover`，现 128 足够，建议 **64×48 免放大** |
| 放大镜搜索图标 | `font-size 3rem`(约30px)，图标字体 | 无（jiayiyingbaoicon iconfont） | 用图标字体，非图片 |

---

## 附图：《各栏目图片速查速记表》（可直接对照替换）

| 用途 | 宽×高(px) | 比例 | object-fit |
|------|-----------|------|------------|
| Hero 大图 | 1920×1080 | 16:9 | cover |
| Banner 横幅 | 1600×460 | ~16:3.6 | cover |
| 产品正方形卡片 | 800×800 (大 1200) | 1:1 | cover |
| 证书竖卡 | 900×1116 | 1:1.24 | contain |
| Application 大图 | 1920×780 | ~2.5:1 | cover |
| 行业图标(双色) | 44×44 (SVG) | 1:1 | contain |
| 新闻卡片 | 1200×675 | 16:9 | cover |
| 产品主图/车间/相关 | 1200×800 | 3:2 | cover |
| 测试大图 | 1600×900 | 16:9 | cover |
| 测试缩略图 | 800×600 | 4:3 | cover |
| CNC 竖图 | 900×1008 | ~0.89 | cover |
| Project 缩略图 | 1280×800 | 16:10 | cover |
| 关于公司视频封面 | 1600×900 | 16:9 | cover |
| Logo | 高≥100 透明 | 任意 | — |

---

> 说明：
> - 凡是 `cover` 的容器，图片比例越接近容器比例越不易被裁，按上表比例制作最稳。
> - 凡是 `contain` 的（证书），图片必须严格按容器比例制作，否则会留白边。
> - 小图标（行业图标）放大必糊，务必用 SVG 或重新按 44px 基座制作高清版。